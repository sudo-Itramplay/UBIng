Sentència [[DDL]] del [[SQL]] per modificar estructures de taules ja existents.

**Referència: Diapositives 74-79**

- **Afegir/eliminar columnes:** `ALTER TABLE taula ADD/DROP COLUMN nom;`
- **Modificar restriccions:** afegir o treure `UNIQUE`, `PRIMARY KEY` o `FOREIGN KEY` a taules ja creades.
- **Valors per defecte:** `ALTER TABLE taula ALTER COLUMN col SET DEFAULT valor;`

Relacionat: [[DDL]] · [[Restriccions SQL]] · [[SQL]]

#assignatura/BD #tipus/teoria
